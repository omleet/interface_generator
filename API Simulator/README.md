The file "mock-api-server.js" simulates a mock API server.

In order to run the mock API server and the dashboard, follow these steps:

1. In a new terminal run the file "mock-api-server.js" with the command: node mock-api-server.js 
2. Then run the dashboard generated with: python -m http.server 8080

Notes: Need Python and node installed.